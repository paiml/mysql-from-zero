/target
**/*.rs.bk
.DS_Store
*.swp
*.tmp
.idea/
.vscode/
*.profraw
lcov.info
/coverage/

# Sakila sample database (downloaded via `make sakila`)
/sakila-db/

# Legacy bind-mount data dir from before compose switched to a named
# volume — kept here so old checkouts don't accidentally re-add it.
/data/
